<Window x:Class="SoftielRemote.Agent.Views.ScreenSharingNotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ekran Paylaşımı" Height="120" Width="400"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        ShowInTaskbar="False"
        Topmost="True">
    
    <Border BorderBrush="#10b981" BorderThickness="2" CornerRadius="8" Background="#0f172a">
        <Grid Margin="15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Icon -->
            <Border Grid.Column="0" 
                    Width="40" Height="40" 
                    Background="#10b981" 
                    CornerRadius="20"
                    Margin="0,0,15,0"
                    VerticalAlignment="Center">
                <TextBlock Text="📺" 
                          FontSize="20" 
                          HorizontalAlignment="Center" 
                          VerticalAlignment="Center"/>
            </Border>
            
            <!-- Content -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock Text="Ekran Paylaşımı Aktif" 
                          Foreground="White" 
                          FontSize="16" 
                          FontWeight="Bold"
                          Margin="0,0,0,5"/>
                <TextBlock x:Name="StatusText" 
                          Text="Uzak bağlantı kuruldu" 
                          Foreground="#94a3b8" 
                          FontSize="12"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>





